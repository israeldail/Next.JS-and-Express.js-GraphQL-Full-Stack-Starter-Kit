{
  "build": {
    "services": [
      {
        "serviceName": "backend",
        "path": "backend",
        "buildCommand": "npm install && npm run build",
        "startCommand": "npm start"
      },
      {
        "serviceName": "frontend",
        "path": "frontend",
        "buildCommand": "npm install && npm run build",
        "startCommand": "npm start"
      }
    ]
  }
}
